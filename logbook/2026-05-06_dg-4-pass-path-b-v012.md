# DG-4 PASS — D1 v0.1.2 Path B numerical verdict (post-supersedure repair)

**Date:** 2026-05-06
**Type:** dg-pass
**Triggering commit:** (to be populated on commit)
**Triggering evidence:**
- Card [D1 v0.1.2](../benchmarks/benchmark_cards/D1_failure-envelope-convergence_v0.1.2.yaml).
- Result artefact [D1_failure-envelope-convergence_v0.1.2_result.json](../benchmarks/results/D1_failure-envelope-convergence_v0.1.2_result.json) (audit-complete: per-α + per-α-per-perturbation `r_4` persisted).
- Repair commits consumed by v0.1.2: `d67b453` (Path B picture fix), `a908cd6` (operational `upper_cutoff_factor`), `5441467` (audit-complete `dg4_sweep_data` + JSON writer).
- Predecessor logbook entries: [`2026-05-06_dg-4-pass-path-b.md`](2026-05-06_dg-4-pass-path-b.md) (v0.1.1 verdict, superseded) and [`2026-05-06_dg-4-pass-path-b-superseded.md`](2026-05-06_dg-4-pass-path-b-superseded.md) (supersedure citation).

## Summary

DG-4 is marked PASS on a v0.1.2 Path B numerical verdict. After repairing the two HIGH-severity defects (Path B picture / `Λ_0⁻¹` similarity error, and trivial PASS-predicate satisfaction for two of four perturbations) and the MEDIUM audit-completeness gap recorded in the v0.1.1 supersedure, the σ_x thermal D1 fixture again identifies a cause-labelled `convergence_failure` regime. All 20 frozen `coupling_strength` sweep points classify as `convergence_failure` under the parity-aware `r_4 = ⟨‖L_4^dis‖⟩_t / ⟨‖L_2^dis‖⟩_t` metric, with stability across **all four** reproducibility perturbations (now genuinely operational under Path B). Maximum baseline `r_4 ≈ 47.42`; minimum perturbed coefficient ratio ≈ 41.47 at `omega_c = 11.0`, comfortably above threshold 1.0.

The verdict consumes the d67b453..5441467 repair commits and is auditable from the result JSON alone (per-α + per-α-per-perturbation `r_4`, per-perturbation Path B fit residual + dissipator-norm coefficients all persisted).

## Detail

The Phase D run used the picture-fixed Path B numerical Richardson extraction. It ran in ~37 s on the steward laptop at runner version `0.1.0`.

Baseline Path B readout (interaction-picture transformed):

| Quantity | Value |
|---|---:|
| Fit relative residual | `3.217e-05` |
| `<‖L_2^dis‖>_t` | `2.742e+01` |
| `<‖L_4^dis‖>_t` | `1.300e+03` |
| Coefficient ratio | `4.742e+01` |
| Maximum baseline `r_4` | `4.742270310996e+01` |

Per-perturbation coefficient ratios (all four operational, all classifications stable):

| Perturbation | Pathway | Coefficient ratio |
|---|---|---:|
| `upper_cutoff_factor = 20` | runner → builder `omega_max_factor` | `4.968e+01` |
| `upper_cutoff_factor = 40` | runner → builder `omega_max_factor` | `4.631e+01` |
| `omega_c = 9.0` | direct model-spec mutation | `5.264e+01` |
| `omega_c = 11.0` | direct model-spec mutation | `4.147e+01` |

The PASS condition is the DG-4 asymmetry: DG-4 passes by finding at least one reproducible, cause-labelled failure regime. Here every swept point classifies as `convergence_failure`, and each remains failing under all four perturbations, so there is no interpolated `α_crit` inside the frozen range. The boundary is below the first swept value, `coupling_strength = 0.05`, under this Path B run.

### What changed from v0.1.1 to v0.1.2

The card structure (model, metric, sweep range, perturbation set) is unchanged. The verdict differs because the *implementation under the card* now satisfies the predicate:

1. **Picture fix** ([`d67b453`](commit history)): Path B coefficients `Λ_2`, `Λ_4` are conjugated by `Ad U†(t)` before the order-4 extractor runs. The `‖L_n^dis‖_F` values are now picture-invariant; the v0.1.1 buggy values were ~5–10% larger because the closed-system L_0 contribution was leaking into the extraction.
2. **Operational `upper_cutoff_factor`** ([`a908cd6`](commit history)): the runner threads `numerical.quadrature.upper_cutoff_factor` into the finite-env builder as `omega_max_factor`. The PASS predicate now genuinely tests four distinct perturbations.
3. **Audit-complete result JSON** ([`5441467`](commit history)): `_run_dg4_sweep` populates `CardResult.dg4_sweep_data` with per-α + per-perturbation `r_4` plus per-perturbation fit residuals; `write_dg4_result_json` persists the full sweep table.

### What still doesn't stand

This verdict does not claim analytic order-4 recursion completion. `cbg.tcl_recursion.L_n_thermal_at_time(n=4)` remains intentionally deferred behind the structured Path A/B/C wall. The D1 v0.1.2 verdict is therefore still a numerical-L_4 failure-envelope result carrying the documented Path B finite-env extraction floor from the 2026-05-06 σ_z thermal zero-oracle pilot. Analytic Path A (Companion Sec. IV closed form) remains preferred for machine-precision L_4 evaluation and future cross-validation.

## Routing notes

The validity envelope is updated to record DG-4 PASS at v0.1.2 with the Path B numerical caveat. CL-2026-005 v0.4 Entry 2 remains scope-limited for analytic recursion, but the open convergence question now has a repository-local failure-envelope witness from the σ_x thermal D1 v0.1.2 sweep that satisfies the v0.1.4 work plan's reproducibility predicate as written.

The v0.5.0 git tag remains the immutable history of the (later-superseded) v0.1.1 PASS commit; whether to bump a new tag for the v0.1.2 verdict is a separate steward decision and out of scope of this entry.

The D1 v0.1.2 card's `result.commit_hash` lands empty in the verdict commit per `SCHEMA.md` §Card lifecycle; a follow-up self-referential commit fills it with this verdict commit's hash.

---

*Logbook entry. Immutable once committed. The `Triggering commit` placeholder above may be filled self-referentially in a follow-up commit per [`logbook/README.md`](README.md) §Immutability exception 2.*
