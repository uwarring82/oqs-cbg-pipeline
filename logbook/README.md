# `logbook/` — Immutable repository event log

The logbook is the append-only record of repository events: initialisation, version bumps, Decision Gate passes (and downgrades), Sail revisions, Ledger imports, structural changes, and significant discussion outcomes.

## Conventions

### File naming

```
logbook/YYYY-MM-DD_<short-tag>.md
```

- `YYYY-MM-DD` is the ISO date of the event in the steward's local timezone (Europe/Berlin).
- `<short-tag>` is a kebab-case tag describing the event. Examples: `repo-init`, `dg-1-pass`, `sail-v0.5-bump`, `ledger-v0.5-imported`, `dg-3-readiness-pass`.
- One file per event. Do not bundle multiple events into a single file even if they share a date.

### Content structure

Each entry contains, at minimum:

```markdown
# <Title>

**Date:** YYYY-MM-DD
**Type:** <one of: initialisation, dg-pass, dg-downgrade, sail-bump, ledger-import,
              structural, discussion-outcome, conflict-flag, other>
**Triggering commit:** <hash, populated after commit>
**Triggering evidence:** <files, cards, tests, or external references>

## Summary

(One paragraph stating what happened.)

## Detail

(Any further detail. Specifically: which DG was affected, what the validity-envelope
change is, any cause labels assigned, any stewardship flags applied or propagated.)

## Routing notes

(If the event has implications for the Ledger or Sail, state the routing here.
Do NOT modify the Ledger or Sail from logbook entries; document the route only.)
```

### Immutability

Once committed, a logbook entry is immutable. Corrections take the form of a *new* entry that supersedes the old one, with an explicit `supersedes: YYYY-MM-DD_old-tag.md` field in its header. The old entry remains in the repository, with a `superseded by:` annotation appended.

Two narrow categories of post-commit edit are permitted:

1. **`superseded by:` annotation** — appended when a successor entry is added (per the supersedure rule above).
2. **Self-referential placeholder fills** — where an entry's `Triggering commit:` field carries the explicit placeholder `(to be populated on commit)` *and* the entry's introducing commit is itself the trigger (typical of sail-bumps, plan-revision logs, and other entries whose subject is the commit they live in). The placeholder may be replaced with the introducing commit's hash in a follow-up commit. The follow-up commit message must state `logbook: fill self-referential triggering-commit placeholder for <entry-tag>` and touch only that field. Any other text edit to a committed entry continues to require supersedure.

Both exceptions are bookkeeping completions of explicit gaps, not substantive revisions. They mirror the Ledger's discipline at the repository layer (the Ledger permits typo-level corrections under Council mandate; the logbook permits these two structural completions under steward discipline).

### What does *not* go in the logbook

- Routine commits (those go in commit messages and PR descriptions).
- Discussion that has not produced an outcome (that goes in issues).
- Minor documentation typo fixes (commit message suffices).
- Anything that belongs in the Ledger or Sail (those have their own change procedures; the logbook may *reference* such changes but does not *enact* them).

## Index

| Date | Tag | Type | Summary |
|---|---|---|---|
| 2026-04-29 | `repo-init` | initialisation | Repository scaffold v0.1.0; Sail v0.4 active; CL-2026-005 v0.4 vendored |
| 2026-04-29 | `public-site-added` | structural | Root `index.html` added as a static public landing page |
| 2026-04-29 | `sail-v0.5-bump` | sail-bump | Cardinality-fix supersedure: Sail v0.5 names all five `docs/` files non-optional, aligning Sail wording with already-enforced behaviour; no Ledger impact |
| 2026-04-30 | `benchmark-card-schema-drafted` | structural | Phase A of DG-1 work plan v0.1.2: `benchmarks/benchmark_cards/SCHEMA.md` (schema v0.1.1) + `_template.yaml` drafted; v0.1.1 bump within Phase A added `model_kind` discriminator after Card A1 preview surfaced misfits; cards-first ordering remains intact |
| 2026-04-30 | `dg-1-cards-frozen` | structural | Phase B of DG-1 work plan v0.1.2: Cards A1, A3, A4 committed with `status: frozen-awaiting-run`; cards-first / Risk #6/#8 mitigation now mechanically auditable via `git log --diff-filter=A`; schema bumped to v0.1.2 mid-Phase-B after A3 preview surfaced bath_state-sweep gap |
| 2026-04-30 | `dg-1-pass` | dg-pass | DG-1 PASSED — Cards A1 v0.1.1, A3 v0.1.1, A4 v0.1.1 reproduce CL-2026-005 v0.4 Entries 1, 3, 4 (unambiguous sub-cases) at machine precision; Entries 1.B.3, 3.B.3, 4.B.2 deferred to DG-2 per plan v0.1.4 §1.1; validity envelope NOT YET ATTEMPTED → PASS; tag `v0.2.0` pending |
| 2026-05-01 | `hayden-sorce-transcription-initiated` | structural | Source-transcription surface added; Hayden-Sorce 2022 pseudo-Kraus formula transcribed for the Entry 1.B.3 DG-2 unblocking path; no code/card/DG status change |
| 2026-05-01 | `dg-2-b1-card-frozen` | structural | DG-2 Card B1 v0.1.0 frozen against the (pre-commit hardened) Hayden-Sorce transcription; three HPTA pseudo-Kraus fixtures freeze the diagonal half of Entry 1.B.3; no runner handlers yet (cards-first), no DG status change |
| 2026-05-01 | `dg-2-b1-pass` | structural | DG-2 Card B1 v0.1.0 PASS — diagonal pseudo-Kraus runner confirms Entry 1.B.3 at machine precision; three test_cases all error=0.0, HPTA residuals below 1e-14; does not constitute full DG-2 PASS |
| 2026-05-01 | `hayden-sorce-transcription-v0.1.1-bumped` | structural | Transcription v0.1.0 → v0.1.1: §4b adds off-diagonal pseudo-Kraus generalization (Letter-derived consequence, source-content unchanged); §7a adds off-diagonal candidate fixture; opens admissible path for off-diagonal Entry 1.B.3 / Entry 1.D card; no code change, no card freeze, no DG status change |
| 2026-05-01 | `dg-2-b2-card-frozen` | structural | DG-2 Card B2 v0.1.0 frozen against transcription v0.1.1; three off-diagonal HPTA + Hermitian-omega fixtures freeze the off-diagonal half of Entry 1.B.3 + Entry 1.D; new card surface (pseudo_kraus_offdiag_operators / pseudo_kraus_offdiag_omega); no runner handlers yet (cards-first), no DG status change |
| 2026-05-01 | `dg-2-b3-card-frozen` | structural | DG-2 Card B3 v0.1.0 frozen — cross-basis structural-identity check (Entry 1.A basis-independence; Sail v0.5 §9 DG-2 universal default); three test_cases reuse A1 / B1 frozen fixtures and assert K agreement under matrix_unit vs su_d_generator (d=2 normalized Pauli) bases; no runner handlers, no su_d_generator builder yet (cards-first), no DG status change |
| 2026-05-04 | `dg-2-b3-pass` | structural | DG-2 Card B3 v0.1.0 PASS — cross-basis structural-identity runner confirms Entry 1.A basis-independence at machine precision; three test_cases all error <= 1.57e-16 under matrix_unit vs su_d_generator (d=2); su_d_generator_basis stub filled, basis-independence handler factory added; does not constitute full DG-2 PASS |
| 2026-05-04 | `dg-2-b2-pass` | structural | DG-2 Card B2 v0.1.0 PASS — off-diagonal pseudo-Kraus runner confirms Entry 1.B.3 off-diagonal half + Entry 1.D at error = 0.0 across all three β=0.5 fixtures; Hermiticity-of-omega + HPTA gates enforced; 3 of 4 DG-2 sub-claims now PASS, only Council-gated coherent-displacement track remains |
| 2026-05-04 | `dg-2-partial-envelope` | dg-pass | Validity envelope updated to DG-2 PARTIAL (3 of 4 sub-claims PASS) — Entry 1.A basis-independence + Entry 1.B.3 diagonal + Entry 1.B.3 off-diagonal/Entry 1.D verified via Cards B1+B2+B3; Entries 3.B.3, 4.B.2 remain Council-gated; DG-1 row's stale Entry 1.B.3 deferral removed; citation of Entries 1.A, 1.B.3, 1.D now supported at v0.2.0+ with attribution to Hayden-Sorce gauge and d=2 basis-independence scope |
| 2026-05-04 | `cbg-appendix-d-transcription-populated` | structural | Colla-Breuer-Gasbarri Appendix-D-routed transcription populated and classified underdetermined: source material contains spin-system parity algebra but no displacement-profile convention; Act 2 remains required over §3.1-§3.4 |
| 2026-05-04 | `dg-2-b4-conv-registry-card-frozen` | structural | DG-2 Card B4-conv-registry v0.1.0 frozen — first card under Council Act 2 (c)-discipline; four test_cases tag the four cleared displacement profiles (delta-omega_c, delta-omega_S, sqrt-J, gaussian) for Entry 3.B.3 (pure-dephasing); cbg/displacement_profiles.py module added with REGISTERED_PROFILES; reporting/benchmark_card.py _DISPLACEMENT_PROFILES wired; runner extension for coherent-displaced bath_state.family deferred to verdict commit (cards-first); 21 new tests; no DG status change |
| 2026-05-04 | `dg-2-b5-conv-registry-card-frozen` | structural | DG-2 Card B5-conv-registry v0.1.0 frozen — σ_x sibling of B4 under the same Council-cleared profile registry; four test_cases tag the same four cleared displacement profiles for Entry 4.B.2 (eigenbasis rotation under σ_x coupling); verdict criterion is absolute Euclidean error on (σ_x, σ_y) transverse vector of K(t), distinguishing from A4 v0.1.1's thermal "zero rotation" check; runner handlers shared with B4 verdict commit (cards-first); 3 new tests; no DG status change |
| 2026-05-04 | `dg-2-b4-conv-registry-pass` | structural | DG-2 Card B4-conv-registry v0.1.0 PASS — first card under post-Act-2 (c)-discipline reaches verdict; cbg.cumulants.D_bar_1 dispatches on the cleared profile registry; cbg.tcl_recursion.K_total_displaced_on_grid added; _run_dynamical carve-out lifted; four (pure_dephasing, displaced_bath_*) handlers registered; all four profiles PASS at machine precision (≤ 8.9e-16) by the parity-class theorem of Eq. (A.39); B5 still pending v0.2.0 supersedure (prediction-text correction) before its verdict; envelope unchanged at DG-2 PARTIAL 3/4 |
| 2026-05-04 | `dg-2-b5-conv-registry-v020-superseded-frozen` | structural | DG-2 Card B5-conv-registry v0.1.0 superseded by v0.2.0 same-day (Schrödinger-picture prediction correction); the v0.1.0 acceptance criterion mistakenly used the interaction-picture rotating-vector form D̄_1(t)·(cos(ωt), -sin(ωt)) for the predicted transverse channel, but Companion Eq. (28) at n=1 has no A_I(τ) integral — Schrödinger-picture K_1 = D̄_1(t)·σ_x is constant-direction with zero σ_y component; v0.2.0 prediction is (b_pred, c_pred) = (D̄_1(t), 0); steward-side correction (no Council deliberation, registry unchanged); v0.1.0 retained at HEAD with status: superseded |
| 2026-05-04 | `dg-2-b5-conv-registry-pass` | structural | DG-2 Card B5-conv-registry v0.2.0 PASS — joint B4/B5 closure achieved; new _dyn_handler_sigma_x_displaced registered under (spin_boson_sigma_x, displaced_bath_*); all four cleared profiles return error = 0.0 EXACTLY (single-source-of-truth pattern: same D̄_1 array drives both K_1 computation and predicted σ_x channel; σ_y zero by Eq. (A.43)-(A.45) parity); all four DG-2 sub-claim families now PASS; envelope update follows atomically per §Update protocol |
| 2026-05-04 | `dg-2-pass-envelope` | dg-pass | Validity envelope updated to DG-2 PASS — 4 of 4 sub-claims PASS under Council Act 2 cleared registry; B1 + B2 + B3 + B4-conv-registry + B5-conv-registry v0.2.0 collectively verify Entries 1.A, 1.B.3 (both halves) + 1.D, 3.B.3 (under registry), 4.B.2 (under registry); citation of Entries 3.B.3 / 4.B.2 supported with explicit profile naming requirement; K_2-K_4 numerical recursion at order ≥ 4 remains the only unattempted DG-2-internal milestone |
| 2026-05-05 | `dg-3-4-5-scoping` | dg-scoping | DG-3/4/5 scoped in cards-first form — C1, C2, D1, and E1 frozen; validity envelope moved DG-3/4/5 from NOT YET ATTEMPTED to SCOPED; no verdict claims |
| 2026-05-05 | `dg-3-phase-c-runner-wired` | structural | DG-3 Phase C partial implementation — cross-method runner branch added; C1 thermal fixture runs exact_finite_env vs qutip_reference and returns clean FAIL at error ≈ 0.548 > 1e-6; C1 displaced and C2 handlers remain deferred; no DG-3 verdict |
| 2026-05-05 | `dg-3-c1-displaced-handler-wired` | structural | DG-3 next deferred handler — C1 displaced delta-omega_c wired in both reference modules; runner registry entry replaces the previous NotImplementedError; both C1 fixtures run end-to-end (thermal: error ≈ 0.293; displaced: error ≈ 0.309; threshold 1e-6); C2 handlers still deferred; no DG-3 verdict |
| 2026-05-05 | `dg-3-c2-thermal-handler-wired` | structural | DG-3 next deferred handler — C2 (spin_boson_sigma_x) thermal wired with σ_x-coupling exact reference and σ_-/σ_+ secular Lindblad QuTiP reference; rates S(±ω_S) sourced from cbg.bath_correlations; QuTiP path reaches Boltzmann at P(↑)=0.1189 vs target 0.1192; runs to clean FAIL (error ≈ 0.538); only C2 displaced handler still deferred; no DG-3 verdict |
| 2026-05-05 | `dg-3-c2-displaced-handler-wired` | structural | DG-3 last deferred handler — C2 displaced delta-omega_c wired (σ_x coupling + coherent-displaced bath); same σ_-/σ_+ Lindblad as C2 thermal plus a time-dependent σ_x classical drive ⟨B(t)⟩ σ_x; runs to clean FAIL (error ≈ 0.526); **all four C1+C2 fixtures now runner-reachable**; next: failure-asymmetry clearance via a third method from a non-overlapping class; no DG-3 verdict |
| 2026-05-05 | `d1-e1-runner-refusal-wired` | structural | Runner-gap closure — `run_card(D1)` raises `DG4SweepRunnerNotImplementedError` (names missing TCL recursion at order ≥ 3 and sweep-block-aware branch); `run_card(E1)` raises `ScopeDefinitionNotRunnableError` surfacing the recorded preconditions; both NotImplementedError subclasses; dispatch precedence covered by tests; DG-4 / DG-5 rows updated; cards remain not-runnable but refusal is now defined and informative |
| 2026-05-06 | `dg-4-phase-b0-leaf-correlations` | structural | DG-4 Phase B.0 — `cbg.bath_correlations.n_point_ordered` for thermal Gaussian ohmic at total order n in {3, 4}; Wick factorisation against existing two_point; pinned convention `times = tau_args + reversed(s_args)`; three convention witnesses (all-left Wick, mixed L/R consistency, Hermiticity reflection); 7 new tests across two commits |
| 2026-05-06 | `dg-4-phase-b1-cumulant-recursion` | structural | DG-4 Phase B.1 — `cbg.cumulants.D_bar` mixed L/R + thermal Gaussian n in {3, 4} via set-partition / Möbius-inversion closed form; D̄_3 = D̄_4 = 0 falsification witnesses pass to machine precision (strongest convention check); 4 new tests |
| 2026-05-06 | `dg-4-phase-b2-tcl-order-3` | structural | DG-4 Phase B.2 (n=3 partial) — `L_n_thermal_at_time(n=3)` returns zero superop (D̄_1 = D̄_3 = 0 ⇒ L_3 = 0 thermal Gaussian); K_n_thermal_on_grid n=3 wired; K_total cap raised to 3; n=4 raises structured NotImplementedError with two-regime physics roadmap; K_3 = 0 verified on A3 (σ_z) and A4 (σ_x) fixtures; 6 new tests; n=4 deferred to focused follow-up |
| 2026-05-06 | `dg-4-phase-b3-partial-dissipator-extraction` | structural | DG-4 Phase B.3 partial — `L_n_dissipator_thermal_at_time` and `L_n_dissipator_norm_thermal_on_grid` for n ∈ {0, 1, 2, 3} with convention `L_n^dissipator := L_n + i[K_n, ·]`. Risk R8 unitary-recovery oracle fires at exact zero for n=0 (gate against sign errors); n=2 σ_z and σ_x both give non-zero dissipator norms (~7); Phase B.4 knob-threading propagates; 8 new tests; n=4 follows the L_4 path. Norm-convention parenthetical superseded by `dg-4-phase-b3-norm-convention-clarification.md` (substance unaffected). |
| 2026-05-06 | `dg-4-phase-b3-norm-convention-clarification` | structural-correction | DG-4 Phase B.3 follow-up — corrects one explanatory number in the original Phase B.3 partial logbook entry. The wrong-sign counterfactual is the Liouville-Frobenius residual `2 · ‖L_0‖_F = 2√2 ≈ 2.828`, NOT `2.0` (which would be the spectral-norm value). The implementation, oracle test, and empirical table in the original entry are all unaffected; only the parenthetical pedagogical number is corrected. |
| 2026-05-06 | `dg-4-path-b-pilot-result` | experimental-result | DG-4 Path B numerical pilot — σ_x thermal `‖L_4^dis‖` measured non-zero (avg `6.21e-1` at baseline `coupling_strength = 0.05`), ~27× above the σ_z thermal zero-oracle finite-env floor of `2.32e-2`. Parity-aware ratio `r_4 ≈ 0.49` at baseline (below convergence-failure threshold of 1, consistent with a sweep needing to reach higher α to find `α_crit`). v0.1.4 metric design empirically validated; D1 v0.1.1 supersedure now drafting-ready. **Numerical pilot result, NOT analytic Phase B.2/B.3 (n=4) completion** — Path A remains the preferred analytic deliverable. |
| 2026-05-06 | `dg-4-phase-c-sweep-runner-wired` | structural | DG-4 Phase C complete — `_run_dg4_sweep` wired in `reporting.benchmark_card`, consuming Path B (`benchmarks.numerical_tcl_extraction.path_b_dissipator_norm_coefficients`) for the L_4 source. `run_card(D1 v0.1.1)` now produces a structured CardResult with verdict (PASS/FAIL/CONDITIONAL), per-α classifications (passing / convergence_failure / truncation_artefact / metric-undefined), and α_crit interpolation. Reproducibility set: `upper_cutoff_factor` ±10 via `_quadrature_kwargs`, `omega_c` ±1 via direct model-spec mutation. 7 new tests; 461 passing. **Not** a Phase D verdict commit; D1 v0.1.1 result block remains empty. |
| 2026-05-06 | `dg-4-phase-a-bis-d1-v011-supersedure` | structural | DG-4 Phase A.bis complete — D1 v0.1.0 superseded by D1 v0.1.1. Active failure-envelope card now targets σ_x thermal with parity-aware `r_4 = <‖L_4^dis‖>_t / <‖L_2^dis‖>_t`, operational reproducibility perturbations (`upper_cutoff_factor` via quadrature allow-list; `omega_c` via model-spec mutation), and the same frozen `coupling_strength` sweep 0.05 → 1.0. Card remains `frozen-awaiting-run`; no DG-4 verdict. |
| 2026-05-06 | `dg-4-pass-path-b` | dg-pass | DG-4 PASS — full frozen D1 v0.1.1 run identifies a `convergence_failure` regime via Path B numerical L_4 extraction. All 20 frozen `coupling_strength` sweep points classify as `convergence_failure`; `alpha_crit` is not bracketed because the boundary lies below 0.05. Verdict carries the documented finite-env floor caveat and the Path-B-specific limitation that `upper_cutoff_factor` is threaded but not consumed by `exact_finite_env`; analytic Path A remains pending. *(Superseded by `dg-4-pass-path-b-superseded` on 2026-05-06 — see next row.)* |
| 2026-05-06 | `dg-4-pass-path-b-superseded` | dg-downgrade | DG-4 PASS verdict (D1 v0.1.1, Path B numerical L_4) **superseded** on review. Two HIGH defects: (i) Path B applies the interaction-picture order-4 formula to raw Schrödinger-picture maps without `Λ_0⁻¹` similarity / `L_0` correction (wrong frame for `H_S ≠ 0`); (ii) PASS predicate trivially satisfied for `upper_cutoff_factor ∈ {20, 40}` because Path B does not consume the knob. MEDIUM audit-completeness gap: per-α + per-perturbation `r_4` not persisted. v0.1.2 supersedure pending Path B repair + operational `omega_max_factor` knob + audit-complete result JSON. v0.5.0 git tag remains as immutable history; the verdict it anchored is downgraded on the record. *(Repaired by `dg-4-pass-path-b-v012` — see next row.)* |
| 2026-05-06 | `dg-4-pass-path-b-v012` | dg-pass | DG-4 PASS at D1 v0.1.2 — picture-fixed Path B Richardson extraction with `omega_max_factor` operational and audit-complete result JSON. All 20 frozen `coupling_strength` sweep points classify as `convergence_failure` under the parity-aware `r_4` metric, stable across all four reproducibility perturbations (`upper_cutoff_factor ∈ {20, 40}`, `omega_c ∈ {9, 11}`); maximum baseline `r_4 ≈ 47.42`; minimum perturbed coefficient ratio `≈ 41.47` (still ~41× above threshold). Consumes repair commits `d67b453` (picture fix), `a908cd6` (operational cutoff), `5441467` (audit-complete sweep payload). Path B finite-env extraction floor caveat carried; analytic Path A remains pending. |
| 2026-05-11 | `review-resolution-execution` | structural | Five external reviews (Kimi, Gemini, Claude, Codex, anonymous structural; ~35 issues across Rounds 1–3) consolidated into a work package and executed across 19 commits. 13 of 14 sequenced workstreams landed (WS-D, WS-A, WS-B, WS-C, WS-E, WS-F, WS-I partial, WS-K, WS-La, WS-G, WS-H, WS-Lc, WS-J) plus four follow-up patches (WS-A2, WS-E2, WS-D2, WS-H2). No DG verdict affected. Quality-gate state at HEAD: `ruff check .`, `black --check`, `mypy`, `pytest -q` (471 passed), `sphinx-build -W` all exit 0. WS-Lb (S6 SPDX / S8 Py3.13 / S10 stub model APIs / S13 GitHub templates) and WS-I L5 (summary-JSON mutability) deferred pending steward decisions. |
| 2026-05-11 | `next-tasks-scoping` | discussion-outcome | Forward-looking scoping entry: companion to `review-resolution-execution`. Catalogues remaining work in three tiers — Tier 1 (five §6 work-package decisions ready for execution: L5 summary-JSON mutability, S6 SPDX, S8 Py3.13, S10 stub model APIs, S13 GitHub templates), Tier 2 (four DG-territory milestones from validity_envelope §"next natural milestones": DG-3 failure-asymmetry, DG-4 Path A, DG-5 scope realisation, K_2–K_4 recursion), Tier 3 (anonymous-review attribution, local .venv editable-install fragility). Includes a recommended sequencing and the rationale for it. Not a verdict; no DG status transitions. |
| 2026-05-11 | `tier-1-execution` | structural | All five Tier-1 items from `next-tasks-scoping` landed: WS-I L5 (Path L5-b — new `benchmarks/results/README.md` index; `DG-1_summary.json` immutable; commit `9f686ba`), WS-Lb S8 step 1 (Python 3.13 added to CI matrix; classifier deferred; commit `a78b203`), WS-Lb S10 (Path S10-a — six callable scope-definition stubs in `models/fano_anderson.py` + `models/jaynes_cummings.py` raising `ScopeDefinitionNotRunnableError`; lazy error-class import to avoid `models → reporting` cycle; commit `733e3ff`), WS-Lb S6 (`# SPDX-License-Identifier: MIT` headers on all 41 `*.py`; commit `509c3c1`), WS-Lb S13 (three GitHub templates encoding cards-first / validity-envelope / DG-cause-label / SPDX discipline; commit `80971e0`). Quality gates preserved: pytest 471 → **473** (+2 S10 contract tests); ruff/black/mypy/sphinx -W all exit 0. Work package is now fully executed for its v0.1.0 draft scope. No DG verdict affected. *S8 step 2 (classifier bump) closed in successor entry below.* |
| 2026-05-11 | `s8-step-2-classifier` | structural | Tier-1 follow-up closure: `pyproject.toml` gains the `Programming Language :: Python :: 3.13` classifier in commit `cf7d66c`, completing the S8 two-step plan. Local 473 pytest under Python 3.13.7 plus clean ruff/black/mypy/sphinx -W substituted for the "wait for CI green" pre-condition; if 3.13 fails on first CI push, `cf7d66c` and `a78b203` revert as one rollback step. The `code-quality` job stays pinned to Python 3.12 by design. No DG verdict affected. **Tier-1 of the work package is now fully closed**, no §6 items remain open. |
| 2026-05-11 | `dg-4-work-plan-v015-tier-2b-scaffold` | discussion-outcome | First Tier-2 item opens: DG-4 work plan v0.1.5 (steward-authored) supersedes v0.1.4 as a Tier-2.B Path A scaffold. Six-phase structure (Transcription → Algebraic implementation → Physics oracles → Public n=4 route → D1 Path A / Path B cross-validation → Tier-2.D handoff) with three-valued Phase-E outcome states (supports / contradicts / inconclusive-with-cause) and explicit routing rules per outcome. **DG-4 PASS at D1 v0.1.2 remains the live verdict**; the plan is post-verdict cross-validation, not re-adjudication. v0.1.4 picks up the permitted `superseded_by:` annotation. Six §3 / §8 steward decisions block Phase B / later code work. |

(This index is updated atomically when new entries are added.)

---

*Last updated: 2026-05-11 (Tier-1 fully closed; DG-4 work plan v0.1.5 opens Tier-2.B Path A scaffold; DG-4 v0.1.2 verdict unchanged; pytest 473). CC-BY-4.0 (see ../LICENSE-docs).*
